Array(45); - 45개의 (empty)빈 배열을 만들어줌

Array.fill()- 배열의 처음부터 끝까지 정적인 값 하나로 채움

empty는 반복문이 불가함 
(Array()의 반환값의 배열은 empty배열이므로 foreach불가)

arr.map(callback(currentValue[, index[, array]])[, thisArg])
반환 값은 callback함수의 결과를 모은 새로운 배열

Array.sort([compareFunction])